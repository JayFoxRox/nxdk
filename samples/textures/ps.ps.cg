struct vOut {
  float2 uv : TEXCOORD0;
};

float4 main(
  vOut I,
  uniform float4 color_scale,
  uniform float4 color_bias,
  uniform sampler2D texture
) : COLOR
{
  return tex2D(texture, I.uv).aaaa * 0.5 + 0.5; //FIXME: * color_scale + color_bias;
}
