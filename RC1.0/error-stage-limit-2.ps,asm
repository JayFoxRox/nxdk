!!RC1.0

{ rgb { discard = zero; } } // Stage 0
{ rgb { discard = zero; } } // Stage 1
{ rgb { discard = zero; } } // Stage 2
{ rgb { discard = zero; } } // Stage 3
{ rgb { discard = zero; } } // Stage 4
{ rgb { discard = zero; } } // Stage 5
{ rgb { discard = zero; } } // Stage 6
{ rgb { discard = zero; } } // Stage 7
{ rgb { discard = zero; } } // Error
{ rgb { discard = zero; } } // Error

out.rgb = zero;
out.a = zero;
